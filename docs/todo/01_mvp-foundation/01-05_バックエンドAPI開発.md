# 01-05 バックエンドAPI開発

**最終更新日時:** 2025-01-XX XX:XX JST

## 概要
Next.js API RoutesとFastAPIで予測実行APIを実装する。

**⚠️ 前提条件:**
- 4パターン（A1/A2/B1/B2）対応の実装が完了している必要がある ✅（完了日時: 2025-11-04 08:58）
- 予測表生成アルゴリズムが4パターンすべてを生成できる必要がある ✅（完了日時: 2025-11-04 08:58）
- AIモデル（6モデル構成）が学習済みである必要がある ✅（完了日時: 2025-11-06）
- モデル読み込みユーティリティ（model_loader.py）が実装済みである必要がある ✅（完了日時: 2025-11-06）
- 特徴量エンジニアリングモジュール（feature_extractor.py）が実装済みである必要がある ✅（完了日時: 2025-11-06）

## 詳細タスク

- [x] Next.js API Routeの基本構造を作成する ✅（完了日時: 2025-11-06）
  - src/app/api/predict/route.tsを作成する
  - POSTメソッドのハンドラーを実装する
  - 基本的なレスポンス構造を実装する

- [x] リクエストバリデーションを実装する ✅（完了日時: 2025-11-06）
  - Zodスキーマを作成する（PredictRequestSchema）
  - roundNumberのバリデーションを実装する（1-9999の整数）
  - n3Rehearsalのバリデーションを実装する（3桁の数字文字列）
  - n4Rehearsalのバリデーションを実装する（4桁の数字文字列）
  - バリデーションエラーのレスポンスを実装する

- [x] 予測表生成処理を統合する ✅（完了日時: 2025-11-06）
  - 回号に基づいて4パターン（A1/A2/B1/B2）の予測表を生成する処理を実装する（⚠️ 前提: 予測表生成アルゴリズムが4パターン対応済みである必要がある）✅
  - N3パターンA1/A2/B1/B2の予測表を生成する
  - N4パターンA1/A2/B1/B2の予測表を生成する
  - 生成エラーのハンドリングを実装する
  - 統合モデル用に4パターンすべてを生成する処理を実装する
  - **実装**: `src/lib/predictor/chart-generator.ts`の`generateAllPatternCharts()`関数

- [x] 特徴量計算処理を統合する ✅（完了日時: 2025-11-06）
  - Pythonモジュール（feature_extractor.py）をFastAPI経由で呼び出す処理を実装する
  - 4パターンの予測表とリハーサル数字から特徴量を計算する処理を実装する
  - パターンID（A1=0, A2=1, B1=2, B2=3）を特徴量として追加する処理を実装する
  - 各数字（0-9）の特徴量を計算する（4パターン×10数字 = 40サンプル）
  - 各組み合わせの特徴量を計算する（最良パターンのみ）
  - 特徴量の正規化・スケーリングを実装する
  - **実装**: `api/main.py`でFastAPIエンドポイント経由で実装

- [x] FastAPI AI推論エンジンの準備をする ✅（完了日時: 2025-11-06）
  - api/ディレクトリを作成する
  - api/main.pyを作成する
  - FastAPIアプリケーションの基本構造を実装する
  - モデル読み込み処理を実装する（起動時に読み込み）
  - **実装**: `api/main.py`、`api/requirements.txt`、`api/run.py`、`api/README.md`

- [x] AI推論エンドポイントを実装する ✅（完了日時: 2025-11-06）
  - /api/predict/axisエンドポイントを実装する
  - /api/predict/combinationエンドポイントを実装する
  - リクエストボディの型定義を実装する（Pydantic）
  - モデル読み込み処理を実装する
  - 特徴量を受け取って推論を実行する処理を実装する
  - スコアを算出する処理を実装する（確率 * 1000）
  - **実装**: `api/main.py`の`/api/predict/axis`と`/api/predict/combination`エンドポイント

- [x] 軸数字予測処理を実装する ✅（完了日時: 2025-11-06）
  - 統合モデル（6つのモデル）を読み込む処理を実装する（⚠️ 前提: AIモデルが学習済みである必要がある）✅
  - **参考実装**: `notebooks/model_loader.py`の`ModelLoader`クラスを使用
  - 軸数字予測モデル（N3/N4のみ）を読み込む処理を実装する
  - 4パターン×各数字（0-9）に対する予測を実行する処理を実装する（40サンプル）
  - 統合モデルで推論を実行し、各パターン×各数字の確率を取得する処理を実装する
  - スコアを算出する処理を実装する（確率 * 1000）
  - 4パターン間で比較し、最も高いスコアのパターンを特定する処理を実装する
  - 最良パターンの軸数字をスコア順にランキングする処理を実装する
  - 結果をフォーマットする処理を実装する（最良パターンID（A1/A2/B1/B2）、各パターンのスコアを含む）
  - 注意: ボックス/ストレートの選択に関係なく同じ軸数字予測モデルを使用する
  - **実装**: `api/main.py`の`predict_axis()`関数

- [x] 組み合わせ予測処理を実装する ✅（完了日時: 2025-11-06）
  - 組み合わせ予測モデル（N3/N4 × ボックス/ストレート）を読み込む処理を実装する
  - **参考実装**: `notebooks/model_loader.py`の`predict_combination()`メソッドを使用
  - 指定された軸数字を含む組み合わせを生成する処理を実装する
  - 各組み合わせの特徴量を計算する処理を実装する
  - ボックス/ストレートの選択に応じて適切なモデルで推論を実行する処理を実装する
  - スコア順にランキングする処理を実装する
  - 注意: ボックス/ストレートでモデルを分ける（順序が重要）
  - **実装**: `api/main.py`の`predict_combination()`関数

- [x] APIレスポンス形式を実装する ✅（完了日時: 2025-11-06）
  - PredictionResult型を定義する
  - N3/N4、ボックス/ストレート、軸数字/組み合わせの構造を実装する
  - 最良パターンID（A1/A2/B1/B2）を追加する処理を実装する
  - 各パターンのスコア（比較用）を追加する処理を実装する
  - generatedAtタイムスタンプを追加する処理を実装する
  - 成功レスポンスのフォーマットを実装する
  - **実装**: `src/app/api/predict/route.ts`でレスポンス形式を実装

- [x] エラーハンドリングを実装する ✅（完了日時: 2025-11-06）
  - バリデーションエラーの処理を実装する（400 Bad Request）
  - データが見つからない場合のエラー処理を実装する（404 Not Found）
  - モデルエラーの処理を実装する（500 Internal Server Error）
  - その他の内部エラーの処理を実装する（500 Internal Server Error）
  - エラーレスポンスのフォーマットを統一する
  - **実装**: `src/app/api/predict/route.ts`でエラーハンドリングを実装

- [x] API統合テストスクリプトを作成する ✅（完了日時: 2025-01-XX）
  - [x] `scripts/test-api.sh`を作成
  - [x] FastAPIサーバーのヘルスチェックテスト
  - [x] 軸数字予測エンドポイントのテスト
  - [x] 組み合わせ予測エンドポイントのテスト
  - [x] package.jsonに`test:api`スクリプトを追加
  - **実装**: `scripts/test-api.sh`を作成済み

- [ ] API統合テストを実装する（手動テスト）
  - [ ] 正常系のテストケースを実行する
  - [ ] 異常系のテストケースを実行する（不正な入力、データなし等）
  - [ ] Postmanまたはcurlで手動テストをする

- [ ] パフォーマンス最適化を実施する
  - レスポンスタイムを測定する（目標5秒以内）
  - モデル読み込みを最適化する（キャッシュ等）
  - 不要な処理を削減する
  - 並列処理が可能な箇所を特定する
