# 01-02 データファイル準備

## 概要
過去実績データ（CSV）と罫線マスターデータ（JSON）を準備し、データ読み込み処理を実装する。

## 詳細タスク

- [ ] past_results.csvファイルを作成する
  - CSVのカラム定義を確認する（round_number, draw_date, n3_winning, n4_winning, n3_rehearsal, n4_rehearsal）
  - 直近100回分のデータを収集する
  - CSVフォーマットを検証する（数値、日付形式等）
  - [ ] 日付計算ロジックの修正（将来対応）
    - ナンバーズは年末年始を除く平日のみ抽選があるため、fetch_past_results.pyの日付計算を修正する
    - 単純な日数差ではなく、平日のみをカウントするロジックを実装する
    - 年末年始（12/29〜1/3あたり）を除外する処理を追加する
    - 現在は影響ないため、後回しでOK

- [x] keisen_master.jsonファイルを作成する
  - 08-keisen-master-creation.mdを参照して作成手順を理解する
  - 紙媒体の罫線ルールを入手する
  - N3用の罫線マスターデータを構造化する
    - 百の位の予測出目ルールを定義する（前回・前々回の組み合わせ×10×10パターン）
    - 十の位の予測出目ルールを定義する（前回・前々回の組み合わせ×10×10パターン）
    - 一の位の予測出目ルールを定義する（前回・前々回の組み合わせ×10×10パターン）
  - N4用の罫線マスターデータを構造化する
    - 千の位の予測出目ルールを定義する（前回・前々回の組み合わせ×10×10パターン）
    - 百の位の予測出目ルールを定義する（前回・前々回の組み合わせ×10×10パターン）
    - 十の位の予測出目ルールを定義する（前回・前々回の組み合わせ×10×10パターン）
    - 一の位の予測出目ルールを定義する（前回・前々回の組み合わせ×10×10パターン）
  - JSONスキーマを定義する（各桁の予測出目ルール）
  - JSON形式のバリデーションを実施する

- [x] keisen_master.jsonの検証を実施する
  - JSON構文エラーがないことを確認する ✓
  - 全ての必須パターンが定義されていることを確認する（N3: 300パターン、N4: 400パターン） ✓
  - 予測出目が0〜9の範囲内であることを確認する ✓
  - 既知の回号で予測表を生成して動作確認する（後で実装時に確認）
  - 過去の実績と照合する（可能な場合）（後で実装時に確認）

- [x] データファイルを配置する
  - data/ディレクトリを作成する ✓
  - past_results.csvをdata/に配置する
  - keisen_master.jsonをdata/に配置する ✓

- [x] CSV読み込みユーティリティを作成する
  - src/lib/data-loader/past-results.tsを作成する ✓
  - CSVパーサーを実装する（Node.js標準機能を使用） ✓
  - 型定義を追加する（RoundNumber, PastResult等） ✓
  - バリデーション機能を含む（回号、日付、当選番号フォーマット） ✓
  - エラーハンドリングを実装する（DataLoadError） ✓
  - 便利関数を実装する（getPastResultByRoundNumber, getPreviousResult等） ✓

- [x] JSON読み込みユーティリティを作成する
  - src/lib/data-loader/keisen-master.tsを作成する ✓
  - JSONファイルを読み込む関数を実装する ✓
  - 型定義を追加する（KeisenMaster, ColumnRules等） ✓
  - バリデーション機能を含む（全桁の存在確認、予測出目の範囲チェック） ✓
  - キャッシュ機能を実装する ✓
  - 予測出目取得関数を実装する（getPredictedDigits） ✓

- [x] データバリデーションを実装する
  - 回号の範囲チェック（1-9999）を実装する ✓（past-results.tsで実装済み）
  - 日付形式のチェックを実装する ✓（past-results.tsで実装済み）
  - 当選番号のフォーマットチェック（3桁/4桁）を実装する ✓（past-results.tsで実装済み）
  - リハーサル数字のNULL許容チェックを実装する ✓（past-results.tsで実装済み）
  - 罫線マスターデータの全桁存在確認 ✓（keisen-master.tsで実装済み）
  - 予測出目の範囲チェック（0-9） ✓（keisen-master.tsで実装済み）

- [x] エラーハンドリングを実装する
  - ファイルが見つからない場合のエラー処理を実装する ✓（両方で実装済み）
  - データフォーマットエラーの処理を実装する ✓（両方で実装済み）
  - エラーメッセージを日本語で表示する ✓（DataLoadErrorクラスで実装済み）

- [ ] データ読み込みのテストを実装する
  - 正常系のテストケースを書く
  - 異常系のテストケースを書く（ファイルなし、不正フォーマット等）
  - テストを実行して確認する
