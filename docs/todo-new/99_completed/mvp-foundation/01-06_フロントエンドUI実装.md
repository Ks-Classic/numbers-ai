# 01-06 フロントエンドUI実装

**最終更新日時:** 2025-01-XX XX:XX JST

## 概要
ユーザーインターフェースを実装し、APIと連携して予測結果を表示する。

**⚠️ 前提条件:**
- バックエンドAPIが実装済みである必要がある ✅（完了日時: 2025-11-06）
- AIモデル（6モデル構成）が学習済みである必要がある ✅（完了日時: 2025-11-06）

## 実装状況

**実装済み:**
- ✅ ホーム画面（`src/app/page.tsx`）
- ✅ 回号入力画面（`src/app/predict/page.tsx`）
- ✅ リハーサル数字入力画面（`src/app/predict/rehearsal/page.tsx`）
- ✅ ローディング画面（`src/app/predict/loading/page.tsx`）
- ✅ 軸選択画面（`src/app/predict/axis/page.tsx`）- 実装済み
- ✅ 予測結果表示画面（`src/app/predict/result/page.tsx`）- 実装済み（サンプルデータ使用）
- ✅ 状態管理（Zustand使用、`src/lib/store.ts`）
- ✅ N3/N4切替タブ
- ✅ ボックス/ストレート切替タブ
- ✅ 軸数字/総合ランキング切替
- ✅ 軸数字候補表示（Accordion形式）
- ✅ 当選番号候補テーブル
- ✅ 総合ランキング表示
- ✅ 手動指定軸機能
- ✅ スコア説明モーダル
- ✅ レスポンシブデザイン対応

**未実装/要更新:**
- [x] API連携処理（完了日時: 2025-01-XX）
  - [x] ローディング画面から`/api/predict`エンドポイントへのAPI呼び出し処理
  - [x] FastAPIサーバー起動確認（環境変数`FASTAPI_URL`の設定）
  - [x] APIレスポンスをZustandストアに保存する処理
- [x] エラーハンドリング（完了日時: 2025-01-XX）
  - [x] APIエラーの表示処理
  - [x] ネットワークエラーの処理
  - [x] タイムアウトエラーの処理
  - [x] ユーザーフレンドリーなエラーメッセージの表示

## 詳細タスク

- [x] ホーム画面を実装する ✅（完了日時: 2025-11-06）
  - src/app/page.tsxを実装する
  - 「新規予測を開始」ボタンを配置する
  - デザインシステムに従ったスタイリングを実装する
  - ナビゲーションリンクを追加する

- [x] 回号入力画面を実装する ✅（完了日時: 2025-11-06）
  - src/app/predict/page.tsxを実装する
  - 4桁の数字入力フィールドを実装する
  - 数字キーボードを自動表示する処理を実装する（`inputMode="numeric"`）
  - 入力バリデーションを実装する（4桁必須）
  - 「次へ」ボタンを実装する（4桁入力時のみ有効化）
  - 入力中の桁数を表示する処理を実装する（入力フィールドで制御）
  - エラーメッセージ表示を実装する

- [x] リハーサル数字入力画面を実装する ✅（完了日時: 2025-11-06）
  - src/app/predict/rehearsal/page.tsxを実装する
  - N4リハーサル入力フィールドを実装する（4桁、各桁独立）
  - N3リハーサル入力フィールドを実装する（3桁、各桁独立）
  - 桁位置のラベルを表示する（千、百、十、一）
  - 自動フォーカス移動を実装する（数字入力時に次へ）
  - Backspaceで前のフィールドへ戻る処理を実装する
  - 入力済み桁の視覚的区別を実装する
  - 「AI予測を実行」ボタンを実装する（全桁入力時のみ有効化）
  - エラーメッセージ表示を実装する

- [x] ローディング画面を実装する ✅（完了日時: 2025-11-06）
  - src/app/predict/loading/page.tsxを実装する
  - プログレスバーを実装する
  - AI分析中のメッセージを表示する
  - 処理ステップの表示を実装する（予測表生成、特徴量計算等）
  - スピナーアニメーションを実装する
  - **注意**: 現在はサンプルデータを使用して軸選択画面へ遷移

- [x] 軸選択画面を実装する ✅（完了日時: 2025-11-06）
  - src/app/predict/axis/page.tsxを実装する
  - N3/N4切替タブを実装する
  - ボックス/ストレート切替タブを実装する
  - 軸数字/総合ランキング切替を実装する
  - 軸数字候補表示を実装する（Accordion形式）
  - 当選番号候補テーブルを実装する
  - 総合ランキング表示を実装する
  - 手動指定軸機能を実装する
  - スコア説明モーダルを実装する
  - **注意**: 現在はサンプルデータを使用

- [x] 予測結果表示画面を実装する ✅（完了日時: 2025-11-06）
  - src/app/predict/result/page.tsxを実装する
  - N3/N4切替タブを実装する
  - 全パターン結果を表示する（パターンA/B別）
  - サンプルデータを使用して動作確認可能
  - **注意**: 現在はサンプルデータを使用、API連携は未実装

- [x] N3/N4切替タブを実装する ✅（完了日時: 2025-11-06）
  - shadcn/uiのTabsコンポーネントを使用する
  - N3タブとN4タブを実装する
  - アクティブタブに応じて結果を切り替える処理を実装する
  - 状態管理を実装する（ZustandまたはuseState）
  - **実装**: `src/app/predict/axis/page.tsx`と`src/app/predict/result/page.tsx`に実装済み

- [x] ボックス/ストレート切替タブを実装する ✅（完了日時: 2025-11-06）
  - サブタブとして実装する
  - ボックスタブとストレートタブを実装する
  - アクティブタブに応じて結果を切り替える処理を実装する
  - **実装**: `src/app/predict/axis/page.tsx`に実装済み

- [x] 軸数字/総合ランキング切替を実装する ✅（完了日時: 2025-11-06）
  - 表示モード切り替えボタンを実装する
  - 軸数字モードの表示を実装する
  - 総合ランキングモードの表示を実装する
  - モード切り替え時の状態管理を実装する
  - **実装**: `src/app/predict/axis/page.tsx`に実装済み

- [x] 軸数字候補表示を実装する ✅（完了日時: 2025-11-06）
  - 軸数字候補リストを表示するコンポーネントを実装する
  - スコア順にランキング表示する処理を実装する
  - Accordionコンポーネントで展開/折りたたみを実装する（カスタム実装）
  - 各軸数字の詳細情報を表示する処理を実装する
  - どのパターンから取得された軸数字かを表示する処理を実装する（パターンID表示）
  - **実装**: `src/app/predict/axis/page.tsx`に実装済み、`source`フィールドでパターンID（A1/A2/B1/B2）を表示

- [x] 当選番号候補テーブルを実装する ✅（完了日時: 2025-11-06）
  - 軸数字候補を展開した際の候補番号テーブルを実装する
  - ランク、番号、スコアを表示するテーブルを実装する
  - スコア順にソートする処理を実装する
  - テーブルのスタイリングを実装する
  - **実装**: `src/app/predict/axis/page.tsx`に実装済み

- [x] 総合ランキング表示を実装する ✅（完了日時: 2025-11-06）
  - 全候補をスコア順に表示するテーブルを実装する
  - ページネーションを実装する（必要に応じて）
  - フィルタリング機能を実装する（オプション）
  - **実装**: `src/app/predict/axis/page.tsx`に実装済み、`viewMode === 'overall'`で表示

- [x] 手動指定軸機能を実装する ✅（完了日時: 2025-11-06）
  - 手動指定軸入力フィールドを実装する（0-9の数字）
  - 「計算」ボタンを実装する
  - 指定軸を含む候補を計算して表示する処理を実装する
  - 結果をテーブルで表示する処理を実装する
  - **実装**: `src/app/predict/axis/page.tsx`に実装済み

- [x] スコア説明モーダルを実装する ✅（完了日時: 2025-11-06）
  - shadcn/uiのDialogコンポーネントを使用する
  - スコアの意味を説明するモーダルを実装する
  - 「スコア説明」ボタンを実装する（スコアをクリックで表示）
  - モーダルの開閉処理を実装する
  - **実装**: `src/app/predict/axis/page.tsx`に実装済み

- [x] 状態管理を実装する ✅（完了日時: 2025-11-06）
  - Zustandストアを作成する（src/lib/store.ts）
  - 回号、リハーサル数字、予測結果を管理する
  - UI状態（アクティブタブ等）を管理する
  - API状態（loading, error）を管理する
  - **実装**: `src/lib/store.ts`に実装済み、`usePredictionStore`を使用

- [x] API連携処理を実装する ✅（完了日時: 2025-01-XX）
  - [x] ローディング画面からAPI呼び出し処理を実装する（`/api/predict`エンドポイント）
  - [x] FastAPIサーバーの起動確認と環境変数設定を確認する
  - [x] APIレスポンスをZustandストアに保存する処理を実装する
  - [x] APIレスポンス形式を既存の型定義に合わせる処理を実装する
  - [x] 4パターン（A1/A2/B1/B2）対応のレスポンス処理を実装する
  - **実装場所**: `src/app/predict/loading/page.tsx`を更新済み
  - **参考**: `src/lib/predictor/predictor.ts`にFastAPI呼び出し処理あり
  - **追加**: Alertコンポーネントを追加（`src/components/ui/alert.tsx`）

- [x] エラーハンドリングを実装する ✅（完了日時: 2025-01-XX）
  - [x] APIエラーの表示を実装する
  - [x] ネットワークエラーの処理を実装する
  - [x] タイムアウトエラーの処理を実装する（30秒タイムアウト）
  - [x] ユーザーフレンドリーなエラーメッセージを表示する
  - [x] 再試行ボタンを実装する
  - **実装場所**: `src/app/predict/loading/page.tsx`に実装済み

- [x] レスポンシブデザインを実装する ✅（完了日時: 2025-11-06）
  - モバイルファーストでデザインする
  - 最小画面サイズ320px×568pxに対応する
  - タブレット・デスクトップのレイアウトを実装する
  - Tailwindのレスポンシブユーティリティを使用する
  - **実装**: すべての画面で実装済み（`md:`、`lg:`プレフィックス使用）

- [x] アクセシビリティ対応を実装する ✅（完了日時: 2025-11-06）
  - セマンティックHTMLを使用する
  - ARIAラベルを適切に設定する（shadcn/uiコンポーネントが対応）
  - キーボード操作に対応する
  - スクリーンリーダー対応を確認する
  - **実装**: shadcn/uiコンポーネントがデフォルトで対応

- [x] アニメーションを実装する ✅（完了日時: 2025-11-06）
  - ローディングアニメーションを実装する（Loader2コンポーネントの`animate-spin`）
  - スムーズなUXを実現する
  - **実装**: `src/app/predict/loading/page.tsx`に実装済み
  - **注意**: Framer Motionは未導入（Tailwindのアニメーションを使用）
